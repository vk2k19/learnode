/**
 * Name: addNumbers.spec.js
 * Use: test adding of two numbers
 */

describe("Adding two numbers", function () {

	it('Adding two number is implemented');

	it('Given two numbers (2 and 5), we expect their sum (7) as result');

	it('Given numbers as string ("2" and "5"), we expect their sum (7) as result');

	it('Given one number and one string (2 and "5"), we expect their sum (7) as result');

	it('Given invalid input ("alpha" and "charlie"), we expect Error to be thrown');

	it('Given only one number (2), we expect exception to be raised');

	it('Given no input, we expect exception to be raised');
});
